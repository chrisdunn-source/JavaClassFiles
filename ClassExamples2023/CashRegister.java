package Chapter5;

public class CashRegister {
    //two properties
    //might contain variables to track the number of each type of bills
    // (or even coins and the overall cash balance
    int countOnes;
    int countFives;
    int countTens;
    int countTwenties;
    int countHundreds;
    double cashBalance; // might be fractional if coins are considered


    //two methods
    //makeChange()- given some input amount and a total bill, produce the amount of change to return
    //calculateBalance() - add up the total cash value in the register
    //cashierLogin() - allows a cashier to sign into the register so it can be used

}
