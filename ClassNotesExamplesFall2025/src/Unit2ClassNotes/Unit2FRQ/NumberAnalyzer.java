package Unit2FRQ;

public class NumberAnalyzer
{
    private int number;

    /**
     * Constructs a NumberAnalyzer object with the given number.
     * @param num the integer to be analyzed.
     */
    public NumberAnalyzer(int num)
    {
        number = num;
    }

    /** PART A
     * Returns true if the number of even digits in the object's number is greater
     * than the number of odd digits; otherwise, returns false.
     * For example, if number is 84512, there are 3 even digits (8, 4, 2) and 2 odd digits (5, 1),
     * so it would return true.
     * Precondition: number is non-negative.
     */
    //public boolean hasMoreEvenThanOdd()
    //{
      //  /* Complete PART A implementation here */
        //return;
    //}

    /** PART B
     * Returns the largest digit found in the object's number.
     * For example, if number is 84512, it returns 8.
     * Precondition: number is non-negative. If number is 0, it returns 0.
     */
    //public int getLargestDigit()
    //{
      //  /* Complete PART B implementation here */
    //}
}