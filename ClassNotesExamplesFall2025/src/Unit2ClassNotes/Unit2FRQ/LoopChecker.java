package Unit2FRQ;

public class LoopChecker
{
    /**
     * PART A
     * Returns a string consisting of all the uppercase characters in the given string,
     * in the order they appear. Returns an empty string if no uppercase characters are found.
     * Precondition: s contains only letter characters (a-z, A-Z).
     */
    public static void extractUppercase(String s)
    {
        /* Complete PART A implementation here */
        return;
    }

    /**
     * PART B
     * The method determines if an integer value n is between 10 (inclusive) and 20 (exclusive)
     * OR if n is an odd number greater than 100.
     * This method must use a compound Boolean expression with logical operators.
     *
     * @param n the integer value to check
     */
    public static void checkNumberCondition(int n)
    {
        /* Complete PART B implementation here */
        return;
    }
}